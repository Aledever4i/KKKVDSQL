CREATE TABLE [dbo].[TotalStateOfPatientRegistration] (
    [ID]                    INT            IDENTITY (0, 1) NOT NULL,
    [PATIENT_REGSTATE_NAME] NVARCHAR (100) NOT NULL,
    PRIMARY KEY CLUSTERED ([ID] ASC)
);

