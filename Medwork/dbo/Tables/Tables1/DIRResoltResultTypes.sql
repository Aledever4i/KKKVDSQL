CREATE TABLE [dbo].[DIRResoltResultTypes] (
    [RecId] INT            IDENTITY (1, 1) NOT NULL,
    [Descr] VARCHAR (1000) NOT NULL,
    CONSTRAINT [PK_DIRResoltResultTypes] PRIMARY KEY CLUSTERED ([RecId] ASC)
);

