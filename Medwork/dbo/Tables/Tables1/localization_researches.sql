CREATE TABLE [dbo].[localization_researches] (
    [ID]            INT            NOT NULL,
    [Lokalizatsiya] NVARCHAR (100) NULL,
    CONSTRAINT [localization_researchesPrimKey] PRIMARY KEY CLUSTERED ([ID] ASC)
);

